(function() {
  var Consumer, Range;

  Range = require('semver').Range;

  module.exports = Consumer = (function() {
    function Consumer(keyPath, versionRange, callback) {
      this.keyPath = keyPath;
      this.callback = callback;
      this.versionRange = new Range(versionRange);
    }

    return Consumer;

  })();

}).call(this);
