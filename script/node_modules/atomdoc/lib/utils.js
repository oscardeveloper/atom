(function() {
  var _;

  _ = require('underscore');

  module.exports = {
    getLinkMatch: function(text) {
      var m;
      if (m = text.match(/\{([\w.]+)\}/)) {
        return m[1];
      } else {
        return null;
      }
    },
    multiplyString: function(string, times) {
      var i, ret, _i;
      ret = '';
      for (i = _i = 0; 0 <= times ? _i < times : _i > times; i = 0 <= times ? ++_i : --_i) {
        ret += string;
      }
      return ret;
    }
  };

}).call(this);
